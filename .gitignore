# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# Espelho do Supabase. O acervo de jogos entra no git — é catálogo público e
# serve de referência. Os arquivos (534 MB) e tudo que tem dado pessoal ficam de
# fora: backup/privado/ guarda os 186 e-mails da tabela profiles.
backup/arquivos/
backup/privado/

# Gerado pelo build em dist/, não versionado — ver scripts/gerar-sitemap.mjs
public/sitemap.xml

# Cache do CLI do Supabase, gerado quando ele roda. Nada nosso.
supabase/.temp/
